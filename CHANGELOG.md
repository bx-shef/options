# change log

## 3.0.7 — 2026-09-26
* .claude/skills/shef-use-logger — новый навык: логирование через модуль shef.problems (Monolog). Проблема в журнал событий и файл с типом и ответственным через трейт LoggerProblems, фабрика для статических методов, что отдавать логгеру (исключение, Result, Error), отладка только администратору. Сигнатуры трейта приведены дословно, классы \Shef\Problems\… сверяет tests/docs_test.php репозитория bx-shef/problems, куда навык раскладывается
* build.sh — сверка порядка записей архива сравнивала имена каталогов со слэшем на конце, а архив собран из вывода find, где слэша нет. Когда имя каталога — начало имени соседа (monolog-pr-html и monolog-pr-html-admin в shef.problems), порядки расходятся: «-» меньше «/». Проверка краснела на правильно собранном архиве. Здесь такого соседства нет, сборка не менялась, архив прежний

## 3.0.6 — 2026-09-26
* Несовместимо: удалены lib/integration/shef/uiclear/ (Events, Manager) и lib/integration/update/events.php — две копии одного: кнопка «Правки ядра» в меню обновлений и подтверждение перед установкой. Обе вели на страницу has-change-files.php, которой в модуле нет, обе не были зарегистрированы (installEvents пуст) и обе ехали на портал. Замена — штатные средства Битрикса: контроль целостности файлов из модуля security и «Проверка системы»
* Utils::getColorMap() больше не зависит от модуля shef.uiclear: ветка с перечислением \Shef\UiClear\Css\Color возвращала ровно те же цвета, что и лежавший рядом запасной набор литералами. Поведение не изменилось, зависимости на модуль в коде больше нет
* Удалён языковой файл lang/ru/lib/integration/shef/uiclear/events.php

## 3.0.5 — 2026-09-23
* include.php подключает def-functions.php. Трейт TraitList\Log, Integration\AEvents и Integration\IBlock\AEntity зовут глобальную _log(), которой до этого не существовало: файл объявлял её, ехал в поставке и не подключался ниоткуда. На портале после Loader::includeModule() function_exists('_log') давал false, и первый же вызов Log::log() падал
* tests/include_test.php — подключение модуля объявляет _log(), _log1() и _pr(); проверяются и типы параметров _log(), которые зовёт трейт
* Навыки: правило 11 — модуль подключается до объявления класса, а не внутри метода. use трейта и extends разбираются PHP при чтении файла, includeModule в теле метода опаздывает. Строка добавлена в примеры shef-new-agent, shef-new-ajax-action, shef-options-traits, shef-options-installer; раньше об этом говорил только shef-new-component
* .claude/skills/shef-use-component — выбор между сервис-классом и Builder по проверяемому признаку («правите class.php в этой задаче» вместо «вынести нельзя»); жизненный цикл, который buildClass() не выполняет, перечислен поимённо; живой объект компонента вынесен под собственный подзаголовок
* .claude/skills/shef-feedback — отзыв пишется и тогда, когда задача кончилась вопросом к человеку, а не результатом
* tests/evals_test.php — expected принимает список навыков: одна задача бывает решаема двумя, и ждать ровно один значит считать промахом верный ответ. <none> вместе с именем навыка и повтор в списке запрещены

## 3.0.4 — 2026-09-23
* .claude/skills/shef-use-component — Builder::buildClass() параметры компоненту не передаёт (@memo в докблоке метода), и жизненный цикл при нём не выполняется: ни initParams(), ни checkRequiredParams(), ни includeModules(). В примере навыка addOptionCollection() стоял в одной цепочке с buildClass(), и читатель мог решить, что параметр дойдёт. Вывод и получение данных разведены на два примера, следствия для публичного метода названы прямо

## 3.0.3 — 2026-09-23
* Навыки: точные контракты вместо отсылок к коду. ShOptionsConfig — глобальный класс без namespace; сигнатуры Log::log(array, bool) и getLogFile(); shef.options подключается в файле class.php до объявления класса компонента; в DoUninstall() нельзя трогать классы своего модуля; в .settings.php у ключа controllers два разных варианта — defaultNamespace и namespaces. По отзывам ИИ-агента с прогона v2: решатель работал в каталоге без shef.options, и там, где навык отсылал вместо того, чтобы сказать, он угадывал и ошибался
* .claude/skills/README.md — правило 10: всё, что надо повторить дословно, приводится в навыке; ссылка «возьмите из shef.options» даётся с точным путём и только как дополнение
* composer.json — branch-alias для dev-main указывал на 2.x-dev, хотя модуль давно 3.x: проект с ограничением ^3.0 не мог взять ветку main

## 3.0.2 — 2026-09-23
* Удаление модуля стирает его настройки: UnInstallDB() зовёт Option::delete() по идентификатору модуля. Раньше b_option оставался нетронутым — строки снятого модуля лежали на портале дальше, а повторная установка молча поднимала прежние значения. Ключ savedata = 'Y' настройки оставляет: уговор ядра на случай формы удаления
* Со страницы настроек убрано предупреждение «Для нормальной работы подбора нужно перевести хранение свойств каталога и sku в отдельное хранилище»: текст про чужую задачу, к библиотеке настроек отношения не имеет. Классы Options\RowInfo и Options\TypeUIAlert остаются — ими пользуются и сам optionsconfig.php, и модули линейки
* build.sh — архив собирается воспроизводимо: время записей берётся у коммита, права приводятся к 644/755, порядок записей задан явно. Раньше в zip уезжало время файлов рабочей копии, и у каждого выходил свой sha256; сверять его с константой в документации было единственным вариантом, и константа устаревала на следующем выпуске. Хеш печатается в итоговой строке сборки
* .claude/skills/ — описания всех навыков переписаны для модулей любого вендора на базе shef.options: на прогоне агент не брал навыки «линейки shef.*» для модуля acme.demo, пять случаев из девяти. Реальные фразы прогона добавлены в evals
* .claude/skills/shef-use-component — новый навык: подключение готового компонента из кода, в том числе без вывода и без ob_start
* .claude/skills/shef-new-component — установка компонента из install/components/ через InstallFiles(); без неё портал компонент не видит
* .claude/skills/shef-new-agent — статические сигнатуры FixUser и пример класса целиком; уборка каталога блокировок при удалении модуля
* .claude/skills/shef-new-module — запрет копировать install/admin/ каталогом: одноимённый файл затирает menu.php ядра
* .claude/skills/shef-feedback — отзыв пишется всегда после задачи с навыком, а не только при замечаниях; операционные навыки заканчиваются этим шагом
* .claude/skills/README.md — правила 8 и 9: описание не привязывает навык к вендору; операционный навык заканчивается разделом «В конце»
* tests/uninstall_test.php — удаление уносит настройки модуля и не трогает чужие
* docs/portal-check.md — сверка sha256 со своей сборкой из тега вместо константы; раздел «Если входа в админку нет» (вход нужен четырём шагам из десяти); как воспроизвести отказ при зависимом модуле; на ядрах, где Application::isUtfMode() возвращает true без условий, шаг про CP1251 недостижим, и «пропущено» — верный ответ
* examples/pid.php — пояснение про «нет /proc и ext-posix» было безусловным и читалось как вывод о стенде; теперь правило отдельно, признаки окружения печатаются отдельно

## 3.0.1 — 2026-09-21
* Код модуля не менялся: состав поставки совпадает с 3.0.0 файл в файл, а содержимым различаются только CHANGELOG.md и install/version.php. Версия поднята, чтобы у навыков и evals был номер, на который можно сослаться
* .claude/skills/ — evals/selection.json у операционных навыков, трейтов и установщика: фразы, по которым навык должен выбираться. Описания shef-options-traits, shef-new-option, shef-options-settings, shef-new-component переписаны под задачу, а не под содержание: на них модель промахивалась
* .claude/skills/shef-feedback — навык-отзыв: агент после задачи записывает, чего в навыке не хватило или что было неверно, в .bitrixsh/feedback/
* .claude/skills/README.md — правила заведения нового навыка: имя, описание под задачу с границей и пределом 1024 символа, evals для операционных
* tests/evals_test.php — оформление evals

## 3.0.0 — 2026-09-18
* Несовместимо: из модуля убран markdown-блок целиком — вкладка «Документация», ajax-контроллер Main\Options\Markdown, класс Options\Markdown\Option, параметр indexDoc у ShOptionsConfig::getInstance(), вендорённый vendor/Michelf (php-markdown и php-smartypants) и js-расширение install/js/shef-options/options-markdown. Документация живёт в репозитории, в поставке остаётся README как readme пакета
* Несовместимо: удалён каталог lib/main/oldoptions/ — 13 файлов объявляли те же FQCN, что и lib/main/options/, но лежали не там, где их ищет автозагрузка. Ядро брало options/, oldoptions/ не брал никто, а на портал каталог ехал
* Несовместимо: минимальная версия PHP — 8.2 (была 8.1). CI гоняет 8.2, 8.3, 8.4 и 8.5
* Constants::getSystemUserId() разбирает настройку строго: (int) превращал пустое значение в 0 («от имени никого»), а '5 62' — в 5, то есть в права пользователя, которого никто не выбирал. Всё, что не целое больше нуля, теперь даёт умолчание Constants::DEFAULT_SYSTEM_USER_ID
* Исправлен класс события в установщике: UnInstallEvents() создавал \Bitrix\Main\Entity\Event вместо \Bitrix\Main\Event, и удаление модуля падало с TypeError, если кто-то подписался на onModuleUnInstall
* Установщик убирает /bitrix/js/shef-options на порталах, обновившихся с версий до 3.0.0: записи в installDir для него больше нет
* CLAUDE.md больше не едет в поставку — это памятка по репозиторию, а не документ модуля
* Тесты: autoload_test.php (класс лежит там, где его ищет ядро; один FQCN — один файл), settings_test.php (ссылки из .settings.php никуда не висят), constants_test.php. assets_test.php переведён на общую обвязку
* Несовместимо: удалены \Shef\Options\Singleton и \Shef\Options\Config — устаревшая пара (первый был помечен @deprecated с указанием замены, второй наследовал его и был подмножеством Options\Config). Живут \Shef\Options\Options\Singleton и \Shef\Options\Options\Config
* Несовместимо: Options\Config::getValue() принимает умолчание вторым аргументом и больше не падает на незаданном ключе — раньше PHP 8 писал «Undefined array key», а возврат null из метода с типом string валил вызов TypeError'ом. Добавлен hasValue(): «ключа нет» и «значение пустое» — разные вещи
* Main\TempFile\Pid::clearDir() заработал: метод начинался с return, всё тело было недостижимо. Теперь убирает из каталога группы блокировки МЁРТВЫХ процессов и возвращает список удалённого. Живые и те, про которые выяснить нечем, не трогаются. Отсюда же самолечение add(): упавший процесс больше не держит группу вечно
* Main\TempFile\Pid::getPath() — путь к своему pid-файлу, раньше его можно было узнать только из remove()
* examples/ — запускаемые примеры: синглетон, реестр настроек, объект от массива, pid-блокировка, трейт PrepareFields. Гоняются и на заглушках, и на живом портале (DOCUMENT_ROOT=...); tests/examples_test.php запускает каждый и сверяет обещания
* phpDoc на строительных блоках: Options\Singleton, Options\Config, Main\TempFile\Pid
* .claude/skills/ — пять skills для агента: строительные блоки, набор трейтов, компоненты и ajax, страница настроек модуля, установка сущностей CRM. Имена классов в них сверяются с репозиторием тестом docs_test.php, оформление — skills_test.php
* build.sh: отсутствие node роняет сборку, если JS или *_test.mjs есть; run_tests сверяет число найденных тестов с числом прогнанных; сверка с git archive идёт по индексу и больше не пропускается на грязном дереве; символическая ссылка под контролем git роняет сборку; не-ASCII имена файлов больше не ломают проверки

## 2.4.0 — 2026-09-18
* Безопасность: ajax-загрузчик документации собирал путь из двух параметров запроса без проверки, и «../» в любом из них уводило чтение за каталог модуля. Путь разрешается в \Shef\Options\Main\Options\Markdown\DocumentPath и сверяется с каталогом модуля ПОСЛЕ разрешения — realpath раскрывает и «..», и символические ссылки
* Безопасность: действие getContent проверяет право на модуль. Спрятать вкладку мало — адрес действия виден в коде страницы
* Безопасность: \Shef\Options\Components\Actions\Normal задавал голый «prefilters» из одного HttpMethod, а этот ключ замещает умолчания ядра целиком — с действия снимались Authentication и Csrf. Теперь Normal — это ровно умолчания ядра
* Несовместимо: умолчание AControllerable::getConfigureActionsDefFilter() — Normal вместо Free. Раньше каждое ajax-действие наследника отвечало без входа на портал и без проверки csrf. Компоненту для гостей Free остаётся, но выбирается явно
* tests/documentpath_test.php — 30 проверок разбора пути, включая символическую ссылку наружу и нулевой байт

## 2.3.0 — 2026-09-18
* Модуль поставляется только в UTF-8. Перекодировка убрана из установщика, вместо неё в DoInstall() проверка кодировки проекта и отказ ставиться на портал в CP1251
* Исправлены короткие теги `<?` в def-functions.php и четырёх ainstaller.php: при short_open_tag=Off классы в них не определялись вовсе, а исходник уходил в браузер
* Возвращён параметр $stopSignal в \Shef\Options\Main\TempFile\Pid::removeByGroup, заявленный в 2.2.16, но в сборку не попавший
* Из UnInstallFiles() убрана проверка машины разработчика, из-за которой удаление файлов могло пропускаться, а на Linux сыпался warning
* Публичные пути фронта выведены в Constants::getPublicCssDir() и getPublicJsDir()
* Ссылки во вкладке «Документация» разбираются по схеме URL: документация переехала в репозиторий, и README ссылается на GitHub
* Исправлено чтение $options['str_replace'] без проверки в TraitList\Tools\PrepareFields::translit — на PHP 8 каждый вызов без этого ключа давал warning
* Возвращён declare(strict_types=1) в 14 файлах, потерянный в сборке 2.2.16
* composer.json: пакет bxshef/options, тип bitrix-module с extra.installer-name, лицензия MIT
* Документация модуля вынесена из поставки в репозиторий

## 2.2.16 — 2023-12-09
* добавлена поддержка сигнала в \Shef\Options\Main\TempFile\Pid::removeByGroup

## 2.2.15 — 2023-12-08
* Добавлена поддержка временных файлов и pid-файла
* Оптимизация \Shef\Options\TraitList\Security\FixUser

## 2.2.14 — 2023-11-11
* Добавлено декодирование/кодирование параметров компонента
* Оптимизация кода

## 2.2.13 — 2023-10-29
* Исправление проверки зависимости при установке модуля

## 2.2.12 — 2023-10-29
* Поддержка в SmartStd \Stringable
* Учет блока registerNamespace для composer и свои классы (modules/shef.options/project-context.php) 
* Поддержка при установке зависимости от версии модулей

## 2.2.11 — 2023-09-20
* исправлена ошибка shef_TAB_EXTENSION_TITLE
* добавлена опция CrmSource

## 2.2.8 — 2023-05-11
* добавлена опция department
* добавлено Utils::getInternalUrl
* добавлено Utils::convertEntityListDepartmentToUserId

## 2.2.7 — 2023-04-21
* Исправлен рендер кода PHP в Option\Markdown\Option

## 2.2.6 — 2023-04-20
* Подготовка к публикации в МП

## 2.2.5 — 2023-04-19
* Добавлен Option\Markdown\Option

## 2.2.4 — 2023-04-18
* Подготовка к публикации в МП
* Инсталятор возвращает кодировку всем файлам в модуле если проект работает в UTF кодировке
* Функционал Правки ядра вынесен из модуля

## 2.2.3 — 2023-04-18
* Новый enum EnumCrmRqPreset
* проработан инсталятор - сущности и UF
* проработаны опции модулей
* поддержка смежных модулей

## 2.2.2 — 2023-03-28
* Оптимизация кода
* Новый enum EnumCrmDealCategory
* Новый enum EnumCrmSmartProcessType

## 2.2.1 — 2023-03-14
* SmartStd поддерживает клонирование, восстановление из массива, преобразование в массив
* Обновлена документация
* Новый enum HL
* Новый enum Iblock
* Новый enum PriceType
* Новый enum Currency
* Новый enum Vat
* Новый enum Measure

## 2.1.2 — 2023-02-16
* Рефакторинг

## 2.1-beta.1 — 2023-02-02
* Initial release

[↑ Содержание](README.md)